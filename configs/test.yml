data:
  dir: "CIFAR100" # directory path or "CIFAR10" or "CIFAR100"
  resize: 32 # Resize factor, integer number
  train:
    augmentation:
    shuffle: True
    batch_size: 128
    n_workers: 4
    pin_memory: True
  val:
    shuffle: False
    batch_size: 64
    n_workers: 1
    pin_memory: True
  test:
    shuffle: False
    batch_size: 1
    n_workers: 1
    pin_memory: True

network:
  backbone: "resnet20"

run:
  loss: crossentropy
  optimizer:
    name: "adam"
    lr: 5.0e-4
    
  epoch: 100
  val_interval: 10
  print_interval: 10
  plot_interval: 1
  figure_interval: 10